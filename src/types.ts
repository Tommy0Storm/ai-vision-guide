/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
*/

// This type is no longer semantically correct but is kept for structural compatibility
// with the useGameAssets hook, which is now inert.
export interface AssetsLoaded {
    all: boolean;
}